import { Request, Response, NextFunction } from "express";

export const verifyAnnouncementIsActive = async (req: Request, res: Response, next: NextFunction) => {
    try {
        
    } catch (error) {
        
    }
}